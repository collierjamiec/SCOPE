import { scopeFaviconLink } from './branding.js';

export const trendsHtml = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SCOPE Trends</title>${scopeFaviconLink}<style>
:root{--ink:#12202e;--muted:#637083;--line:#dde4e7;--paper:#f6f7f4;--card:#fff;--teal:#0c7067;--lime:#c7ff65;--navy:#102d3d;--orange:#f28c28;--red:#ba3b46}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 90% 0,#dff8ee,transparent 28%),var(--paper);color:var(--ink);font:15px/1.5 Inter,system-ui,sans-serif}header{padding:24px clamp(20px,5vw,72px);display:flex;justify-content:space-between;align-items:center}.brand{display:flex;align-items:center;gap:11px;font-weight:950;letter-spacing:.13em;color:var(--teal)}.scope-mark{position:relative;display:inline-grid;place-items:center;width:32px;height:32px;border:2px solid var(--orange);border-radius:50%;color:var(--orange);flex:0 0 auto}.scope-mark:before,.scope-mark:after{content:"";position:absolute;background:currentColor}.scope-mark:before{width:42px;height:2px;left:-7px;top:13px}.scope-mark:after{width:2px;height:42px;left:13px;top:-7px}.scope-mark i{position:relative;z-index:1;width:7px;height:7px;border-radius:50%;background:var(--orange);box-shadow:0 0 0 4px var(--paper)}a{color:var(--teal)}main{max-width:1380px;margin:auto;padding:30px clamp(20px,5vw,72px) 80px}h1{font-size:clamp(40px,6vw,72px);letter-spacing:-.05em;line-height:1;margin:12px 0}p.lead{color:var(--muted);font-size:18px}.toolbar,.cards,.charts{display:grid;gap:12px}.toolbar{grid-template-columns:minmax(260px,1fr) auto auto;align-items:end;margin:28px 0}.field{display:grid;gap:5px;font-weight:800}.field select,.field input,.field textarea,.search{padding:11px;border:1px solid var(--line);border-radius:10px;background:white;font:inherit}button,.button{border:0;border-radius:10px;padding:11px 15px;background:var(--navy);color:white;font-weight:850;cursor:pointer}.danger{background:#fdebed;color:var(--red)}.domain-choice{display:flex;align-items:center;justify-content:space-between;gap:18px;min-height:55px;padding:9px 12px;border:1px solid var(--line);border-radius:12px;background:white;text-align:left;color:var(--ink)}.domain-choice strong{display:block}.domain-choice small{display:block;color:var(--muted);font-weight:600}.domain-choice .change{color:var(--teal);white-space:nowrap}.cards{grid-template-columns:repeat(5,1fr);margin:18px 0}.card,.panel{background:white;border:1px solid var(--line);border-radius:16px;box-shadow:0 9px 28px #183b4415}.card{position:relative;padding:16px 16px 38px}.card b{display:block;font-size:27px}.card>span:not(.metric-source){color:var(--muted);font-size:12px;text-transform:uppercase}.card .metric-source{position:absolute;right:11px;bottom:10px;display:grid;place-items:center;width:19px;height:19px;border:1px solid #9db0b8;border-radius:50%;color:#0c7067;font:800 12px/1 Georgia,serif;cursor:help}.card .metric-source:focus-visible{outline:2px solid #f28c28;outline-offset:2px}.charts{grid-template-columns:1fr 1fr}.panel{padding:20px;margin:15px 0;overflow:auto}.panel h2{margin:0 0 12px}.diagnosis{border-left:5px solid #f28c28}.diagnosis-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px}.diagnosis ul{margin:6px 0;padding-left:20px}.chart{width:100%;height:230px}.legend{display:flex;gap:14px;flex-wrap:wrap;color:var(--muted);font-size:12px}.dot{display:inline-block;width:9px;height:9px;border-radius:50%;margin-right:5px}table{border-collapse:collapse;width:100%;min-width:880px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--line);vertical-align:top}th{font-size:12px;text-transform:uppercase;color:var(--muted)}.badge{display:inline-block;padding:3px 8px;border-radius:99px;background:#e6f4ef;color:var(--teal);font-size:12px;font-weight:850}.warn{background:#fff0d5;color:#805100}.empty{padding:50px;text-align:center;color:var(--muted)}.connectivity-strip{position:fixed;right:18px;bottom:18px;z-index:30;display:flex;gap:7px;padding:7px;border:1px solid var(--line);border-radius:99px;background:#ffffffed;box-shadow:0 8px 26px #102d3d33}.connectivity-item{display:grid;place-items:center;width:36px;height:36px;padding:7px;border:2px solid white;border-radius:50%;background:#d09219;color:white;cursor:help}.connectivity-item svg{width:20px;height:20px;fill:currentColor}.connectivity-item.green{background:#17855d}.connectivity-item.yellow{background:#d09219}.connectivity-item.red{background:#ba3b46}.connectivity-item:focus-visible{outline:3px solid #f28c28;outline-offset:2px}.scope-tooltip{position:fixed;z-index:2000;display:none;max-width:min(320px,calc(100vw - 16px));padding:9px 11px;border-radius:8px;background:#102d3d;color:white;font:600 12px/1.4 Inter,system-ui,sans-serif;box-shadow:0 8px 24px #102d3d40;pointer-events:none}dialog{border:0;border-radius:17px;padding:24px;width:min(520px,calc(100% - 30px));box-shadow:0 25px 80px #102d3d66}dialog.wide{width:min(850px,calc(100% - 30px))}dialog::backdrop{background:#102d3daa}.picker-head{display:flex;align-items:start;justify-content:space-between;gap:18px}.picker-head h2{margin:0}.icon-close{padding:4px 9px;background:transparent;color:var(--ink);font-size:22px}.search{width:100%;margin:18px 0}.picker-table{min-width:620px}.picker-row{cursor:pointer}.picker-row:hover,.picker-row:focus-within{background:#f0f8f5}.picker-select{background:transparent;color:var(--teal);padding:5px}.selected-row{background:#eef9f5}.actions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}@media(max-width:900px){.cards{grid-template-columns:repeat(2,1fr)}.charts,.toolbar,.diagnosis-grid{grid-template-columns:1fr}}
</style></head><body><header><div class="brand"><span class="scope-mark" aria-hidden="true"><i></i></span><span>SCOPE / TRENDS</span></div><a href="/">← Current audit dashboard</a></header><main><div class="eyebrow">Historical site intelligence</div><h1>See what changed.</h1><p class="lead">Compare retained audits, distinguish new and resolved findings, and follow slow-moving technical and search-health signals.</p><div id="disabled"></div><div class="toolbar"><label class="field">Domain<button class="domain-choice" id="choose-domain" type="button"><span><strong id="selected-domain">Loading history…</strong><small id="selected-domain-meta">Select a retained domain</small></span><span class="change">Choose domain →</span></button></label><button id="merge">Merge domains</button><button class="danger" id="delete-domain">Delete all history</button></div><div id="view"></div></main><div class="db-status" id="db-status" tabindex="0" role="status" aria-label="Checking database status" title="Checking database status"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2C7.58 2 4 3.34 4 5v14c0 1.66 3.58 3 8 3s8-1.34 8-3V5c0-1.66-3.58-3-8-3Zm0 2c3.87 0 6 .93 6 1s-2.13 1-6 1-6-.93-6-1 2.13-1 6-1Zm0 16c-3.87 0-6-.93-6-1v-2.08C7.47 17.6 9.62 18 12 18s4.53-.4 6-1.08V19c0 .07-2.13 1-6 1Zm0-4c-3.87 0-6-.93-6-1v-2.08C7.47 13.6 9.62 14 12 14s4.53-.4 6-1.08V15c0 .07-2.13 1-6 1Zm0-4c-3.87 0-6-.93-6-1V8.92C7.47 9.6 9.62 10 12 10s4.53-.4 6-1.08V11c0 .07-2.13 1-6 1Z"/></svg></div><dialog id="modal"><h2 id="modal-title"></h2><div id="modal-body"></div><div class="actions"><button onclick="modal.close()">Cancel</button><button class="danger" id="confirm-action">Confirm irreversible action</button></div></dialog><dialog class="wide" id="domain-picker"><div class="picker-head"><div><h2>Choose domain history</h2><div class="lead">Search retained domains and open their trend dashboard.</div></div><button class="icon-close" aria-label="Close domain chooser" onclick="this.closest('dialog').close()">×</button></div><input class="search" id="domain-search" type="search" placeholder="Search by domain…" aria-label="Search domain histories"><div id="domain-results"></div></dialog><dialog class="wide" id="merge-picker"><div class="picker-head"><div><h2>Merge domain histories</h2><div class="lead" id="merge-help"></div></div><button class="icon-close" aria-label="Close merge dialog" onclick="this.closest('dialog').close()">×</button></div><input class="search" id="merge-search" type="search" placeholder="Search for the source domain…" aria-label="Search source domains"><div id="merge-results"></div><label class="field">Reason for merging<textarea id="merge-reason" required placeholder="For example: Domain migrated from www to non-www"></textarea></label><div class="actions"><button onclick="document.querySelector('#merge-picker').close()">Cancel</button><button id="confirm-merge" disabled>Merge selected domain</button></div></dialog><script>
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let domains=[],trend=null,mergeSourceId=null;const fmt=v=>v==null?'—':Number(v).toFixed(1),date=v=>new Date(v).toLocaleString(),domainRun=d=>d.runs?.[0];
const databaseIndicator=$('#db-status'),connectivityStrip=document.createElement('aside');connectivityStrip.className='connectivity-strip';connectivityStrip.setAttribute('aria-label','SCOPE service connectivity');databaseIndicator.replaceWith(connectivityStrip);databaseIndicator.className='connectivity-item yellow';databaseIndicator.removeAttribute('title');databaseIndicator.dataset.tooltip='Checking database status';connectivityStrip.append(databaseIndicator);connectivityStrip.insertAdjacentHTML('beforeend','<button class="connectivity-item yellow" id="ga4-status-icon" type="button" aria-label="Checking GA4 connectivity" data-tooltip="Checking GA4 connectivity"><svg viewBox="0 0 24 24"><path d="M4 15h4v7H4v-7Zm6-6h4v13h-4V9Zm6-7h4v20h-4V2Z"/></svg></button><button class="connectivity-item yellow" id="gsc-status-icon" type="button" aria-label="Checking GSC connectivity" data-tooltip="Checking GSC connectivity"><svg viewBox="0 0 24 24"><path d="M10.5 3a7.5 7.5 0 1 0 4.72 13.33L20.9 22l1.1-1.1-5.67-5.68A7.5 7.5 0 0 0 10.5 3Zm0 2a5.5 5.5 0 1 1 0 11 5.5 5.5 0 0 1 0-11Z"/></svg></button><button class="connectivity-item yellow" id="pagespeed-status-icon" type="button" aria-label="Checking PageSpeed configuration" data-tooltip="Checking PageSpeed configuration"><svg viewBox="0 0 24 24"><path d="M12 3a9 9 0 1 0 9 9h-2a7 7 0 1 1-2.05-4.95L13 11H7v2h6.83l4.54-4.54A8.96 8.96 0 0 0 12 3Z"/></svg></button><button class="connectivity-item yellow" id="seranking-status-icon" type="button" aria-label="Checking SE Ranking connectivity" data-tooltip="Checking SE Ranking connectivity"><svg viewBox="0 0 24 24"><path d="M4 4h16v3H7v3h10a3 3 0 0 1 3 3v4a3 3 0 0 1-3 3H4v-3h13v-4H7a3 3 0 0 1-3-3V4Z"/></svg></button>');const setConnectivity=(selector,color,label)=>{const item=$(selector);item.className='connectivity-item '+color;item.dataset.tooltip=label;item.setAttribute('aria-label',label)};
function line(values,color,max){const width=600,height=190,pad=22,points=values.map((v,i)=>{const x=pad+i*(width-pad*2)/Math.max(1,values.length-1),y=height-pad-(Number(v)||0)*(height-pad*2)/Math.max(1,max);return x+','+y}).join(' ');return '<polyline fill="none" stroke="'+color+'" stroke-width="3" points="'+points+'"/>'}function chart(series){const all=series.flatMap(s=>s.values),max=Math.max(1,...all.map(Number));return '<svg class="chart" viewBox="0 0 600 190" role="img">'+series.map(s=>line(s.values,s.color,max)).join('')+'</svg><div class="legend">'+series.map(s=>'<span><i class="dot" style="background:'+s.color+'"></i>'+esc(s.label)+'</span>').join('')+'</div>'}
function domainRows(items,selectable=false){if(!items.length)return '<div class="empty">No matching domains.</div>';return '<table class="picker-table"><thead><tr><th>Domain</th><th>Audits</th><th>Most recent audit</th><th>Pages</th><th></th></tr></thead><tbody>'+items.map(d=>{const latest=domainRun(d);return '<tr class="picker-row '+(selectable&&d.id===mergeSourceId?'selected-row':'')+'"><td><strong>'+esc(d.displayDomain)+'</strong><br><small>'+esc(d.normalizedDomain)+'</small></td><td>'+d._count.runs+'</td><td>'+(latest?date(latest.generatedAt):'—')+'</td><td>'+(latest?.pageCount??'—')+'</td><td><button class="picker-select" data-'+(selectable?'merge-source':'choose-domain')+'="'+d.id+'">'+(selectable&&d.id===mergeSourceId?'Selected':'Select')+'</button></td></tr>'}).join('')+'</tbody></table>'}
function filterDomains(query,excludeCurrent=false){const value=query.trim().toLowerCase();return domains.filter(d=>(!excludeCurrent||d.id!==trend?.id)&&(!value||d.displayDomain.toLowerCase().includes(value)||d.normalizedDomain.toLowerCase().includes(value)))}
function renderDomainPicker(){const items=filterDomains($('#domain-search').value);$('#domain-results').innerHTML=domainRows(items);document.querySelectorAll('[data-choose-domain]').forEach(b=>b.onclick=async()=>{$('#domain-picker').close();await loadTrend(b.dataset.chooseDomain)})}
function renderMergePicker(){const items=filterDomains($('#merge-search').value,true);$('#merge-results').innerHTML=domainRows(items,true);document.querySelectorAll('[data-merge-source]').forEach(b=>b.onclick=()=>{mergeSourceId=b.dataset.mergeSource;$('#confirm-merge').disabled=false;renderMergePicker()})}
function updateSelectedDomain(){const summary=domains.find(d=>d.id===trend?.id),latest=domainRun(summary||{});$('#selected-domain').textContent=summary?.displayDomain||trend?.normalizedDomain||'Choose domain';$('#selected-domain-meta').textContent=summary?(summary._count.runs+' retained audit'+(summary._count.runs===1?'':'s')+(latest?' · latest '+date(latest.generatedAt):'')):'Select a retained domain'}
function updateDatabaseStatus(status){const color=status.state==='history_available'?'green':status.state==='awaiting_baseline'?'yellow':'red',label=(color==='green'?'Database healthy: ':color==='yellow'?'Database ready: ':'Database unavailable: ')+(status.message||'Status unavailable');setConnectivity('#db-status',color,label)}
async function loadConnectivity(){const results=await Promise.allSettled([fetch('/api/google-analytics/status').then(r=>r.json()),fetch('/api/google/status').then(r=>r.json()),fetch('/api/pagespeed/status').then(r=>r.json()),fetch('/api/seranking/status?refresh=1').then(r=>r.json())]),value=index=>results[index].status==='fulfilled'?results[index].value:null,ga4=value(0),gsc=value(1),speed=value(2),seranking=value(3);setConnectivity('#ga4-status-icon',!ga4?'red':ga4.connected?(ga4.propertyAccessReady?'green':'yellow'):'yellow',!ga4?'GA4 status unavailable':ga4.connected?(ga4.propertyAccessReady?'GA4 connected; '+(ga4.properties?.length??0)+' properties available.':'GA4 account connected; property access needs attention. '+(ga4.recovery?.message??'No accessible GA4 properties were found.')):ga4.configured?'GA4 OAuth client saved; connect a Google account.':'GA4 not configured; CSV fallback remains available.');setConnectivity('#gsc-status-icon',!gsc||gsc.error?'red':gsc.connected?'green':'yellow',!gsc?'GSC status unavailable':gsc.connected?'GSC connected; '+(gsc.sites?.length??0)+' properties available.':gsc.configured?'GSC OAuth client saved; connect a Google account.':'GSC not configured; CSV fallback remains available.');setConnectivity('#pagespeed-status-icon',!speed?'red':speed.configured?'green':'yellow',!speed?'PageSpeed status unavailable':speed.configured?'PageSpeed Insights API key stored locally; live quota is checked during an audit.':'No PageSpeed API key stored; anonymous quota may be limited.');setConnectivity('#seranking-status-icon',!seranking||seranking.error?'red':seranking.connected?'green':'yellow',!seranking?'SE Ranking status unavailable':seranking.connected?'SE Ranking connected; '+(seranking.projects?.length??0)+' projects available.':seranking.configured?'SE Ranking key saved; connection not confirmed.':'SE Ranking not configured; CSV import remains available.')}
async function loadDiagnosticHealth(){try{const health=await fetch('/api/diagnostics/health').then(r=>r.json()),map={database:'#db-status',ga4:'#ga4-status-icon',gsc:'#gsc-status-icon',pagespeed:'#pagespeed-status-icon',seranking:'#seranking-status-icon'};for(const [name,selector] of Object.entries(map)){const state=health[name];if(state?.state==='error')setConnectivity(selector,'red',state.message+(state.resolution?' How to fix: '+state.resolution:''))}}catch{}}
function metricInfo(label){return '<span class="metric-source" tabindex="0" role="button" aria-label="'+esc(label)+'" data-tooltip="'+esc(label)+'">i</span>'}
function trendCard(value,label,source){return '<div class="card"><b>'+value+'</b><span>'+esc(label)+'</span>'+metricInfo(source)+'</div>'}
function period(start,end){return start&&end?new Date(start).toLocaleDateString()+'–'+new Date(end).toLocaleDateString():'dates unavailable'}
function actionFor(issue){const rule=issue.ruleId.toLowerCase();if(rule.includes('broken'))return 'Repair or replace the affected links and verify their final HTTP status.';if(rule.includes('canonical'))return 'Align canonicals with the intended indexable URL and remove conflicting signals.';if(rule.includes('schema'))return 'Correct or add page-appropriate structured data and validate required properties.';if(rule.includes('orphan')||rule.includes('internal'))return 'Add relevant contextual internal links from authoritative, discoverable pages.';if(rule.includes('title')||rule.includes('description')||rule.includes('h1'))return 'Write unique, intent-aligned metadata and headings for the affected page set.';if(rule.includes('image')||rule.includes('alt'))return 'Correct missing image alternatives and unhelpful asset names where appropriate.';if(issue.category==='aio'||issue.category==='geo')return 'Strengthen direct answers, entity clarity, supporting evidence, and machine-readable context.';return 'Review representative affected URLs, define the intended outcome, and fix the shared template or content pattern.'}
function trendCards(runs,last){const generated='Latest retained SCOPE audit: '+date(last.generatedAt),gsc='Based on '+(last.gscKeywordCount??'an unavailable number of')+' observed ranking keywords for '+period(last.gscPeriodStart,last.gscPeriodEnd);return '<div class="cards">'+trendCard(runs.length,'Retained runs','Stored historical audits for '+trend.normalizedDomain)+trendCard(last.pageCount,'Latest pages',generated)+trendCard(last.orphanPageCount,'Orphan pages',generated+' · pages with no discovered incoming internal link')+trendCard(fmt(last.schemaCoverage)+'%','Schema coverage',generated+' · analyzed pages with appropriate valid schema')+trendCard(fmt(last.gscAveragePosition),'Average GSC position',gsc)+'</div>'}
function historicalExecutive(runs,last){const first=runs[0],diagnosis=trend.trafficDiagnosis,persistent=trend.persistentIssues??[],criticalChange=last.criticalCount-first.criticalCount,warningChange=last.warningCount-first.warningCount,healthDirection=runs.length<2?'Baseline established':criticalChange<0||warningChange<0?'Technical health is improving':criticalChange>0||warningChange>0?'Technical issue volume increased':'Technical issue volume is broadly stable',quick=persistent.slice(0,4),evidence=(diagnosis.evidence??[]).map(item=>'<li>'+esc(item)+'</li>').join(''),next=(diagnosis.nextSteps??[]).map(item=>'<li>'+esc(item)+'</li>').join('');return '<section class="panel diagnosis"><div class="eyebrow">Historical executive summary</div><h2>'+esc(diagnosis.headline)+'</h2><p><b>'+esc(healthDirection)+'.</b> '+esc(diagnosis.explanation)+'</p><div class="diagnosis-grid"><div><h3>What the trend data suggests</h3>'+(evidence?'<ul>'+evidence+'</ul>':'<p>A second distinct, date-bounded comparison period is needed before traffic direction can be diagnosed.</p>')+'<p><span class="badge '+(diagnosis.confidence==='low'?'warn':'')+'">'+esc(diagnosis.confidence)+' confidence</span> This is a diagnosis from observed SCOPE, GSC, and GA4 evidence—not proof of seasonality, an algorithm update, or competitor activity.</p><h3>Quick wins</h3>'+(quick.length?'<ol>'+quick.map(issue=>'<li><b>'+esc(issue.message)+'</b><br><small>'+issue.affectedPages+' currently affected page'+(issue.affectedPages===1?'':'s')+' · seen in '+issue.observedRuns+' audits since '+new Date(issue.firstSeen).toLocaleDateString()+'</small><br>'+esc(actionFor(issue))+'</li>').join('')+'</ol>':'<p>No issue pattern has persisted across multiple retained audits yet. Use the latest report priorities until another comparable audit establishes duration.</p>')+'</div><div><h3>Go deeper: solve the business problem</h3><ul>'+next+'<li>Connect query and landing-page changes to leads, revenue, subscriptions, or other business outcomes instead of optimizing traffic in isolation.</li><li>For answer-engine/AIO opportunities, evaluate whether priority pages answer the real question directly, identify entities clearly, cite credible evidence, and support useful follow-up questions.</li></ul><h3>Unresolved patterns</h3><p>'+persistent.length+' rule pattern'+(persistent.length===1?' has':'s have')+' remained present across multiple retained audits. Prioritize recurring critical and warning patterns that affect valuable landing pages before broad cosmetic work.</p></div></div></section>'}
async function loadDomains(){void loadConnectivity().then(loadDiagnosticHealth);const status=await fetch('/api/v1/trends/status').then(r=>r.json()),response=await fetch('/api/v1/trends/domains'),data=await response.json();updateDatabaseStatus(status);void loadDiagnosticHealth();$('#disabled').innerHTML='';if(!response.ok){$('.toolbar').style.display='none';$('#view').innerHTML='<div class="panel empty"><b>Historical data cannot be loaded.</b><br>Check the connectivity indicators in the bottom-right corner for details.</div>';return}domains=data;if(data.length){let domainId=data[0].id;const runId=new URLSearchParams(location.search).get('run');if(runId){const located=await fetch('/api/v1/trends/runs/'+encodeURIComponent(runId)).then(r=>r.ok?r.json():null);if(located?.domainId)domainId=located.domainId}loadTrend(domainId)}else{$('#selected-domain').textContent='No stored audit history';$('#selected-domain-meta').textContent='Complete an audit to create a baseline';$('#choose-domain').disabled=true;$('#merge').disabled=true;$('#delete-domain').disabled=true;$('#view').innerHTML='<div class="panel empty"><b>Ready for the first baseline.</b><br>Complete any audit; SCOPE will retain it automatically as the historical baseline. A later audit of the same domain will show changes.</div>'}}
async function loadTrend(id){trend=await fetch('/api/v1/trends/domains/'+id).then(r=>r.json());updateSelectedDomain();history.replaceState(null,'','/trends?domain='+encodeURIComponent(id));render()}function delta(run,state){return run.deltas.filter(d=>d.state===state).length}function render(){const runs=trend.runs,last=runs.at(-1);if(!last){$('#view').innerHTML='<div class="panel empty">No runs retained.</div>';return}$('#view').innerHTML=trendCards(runs,last)+historicalExecutive(runs,last)+'<div class="charts"><section class="panel"><h2>Findings by severity</h2>'+chart([{label:'Critical',color:'#ba3b46',values:runs.map(r=>r.criticalCount)},{label:'Warning',color:'#b76a13',values:runs.map(r=>r.warningCount)},{label:'Info',color:'#2f7fbd',values:runs.map(r=>r.infoCount)}])+'</section><section class="panel"><h2>Finding movement</h2>'+chart([{label:'Opened',color:'#b76a13',values:runs.map(r=>delta(r,'OPENED'))},{label:'Resolved',color:'#0c7067',values:runs.map(r=>delta(r,'RESOLVED'))},{label:'Reopened',color:'#ba3b46',values:runs.map(r=>delta(r,'REOPENED'))},{label:'Persisting',color:'#637083',values:runs.map(r=>delta(r,'PERSISTING'))}])+'</section><section class="panel"><h2>Search and structure</h2>'+chart([{label:'GSC average position',color:'#f28c28',values:runs.map(r=>r.gscAveragePosition??0)},{label:'Average click depth',color:'#0c7067',values:runs.map(r=>r.averageClickDepth??0)},{label:'Orphan pages',color:'#ba3b46',values:runs.map(r=>r.orphanPageCount)}])+'</section><section class="panel"><h2>Coverage and performance</h2>'+chart([{label:'Schema coverage %',color:'#0c7067',values:runs.map(r=>r.schemaCoverage??0)},{label:'Crawlable/indexable rate %',color:'#2f7fbd',values:runs.map(r=>r.indexableRate??0)},{label:'Average LCP ms',color:'#f28c28',values:runs.map(r=>r.averageLcp??0)}])+'</section></div><section class="panel"><h2>Audit runs</h2><table><thead><tr><th>Date</th><th>Scan</th><th>Pages</th><th>Critical / warning / info</th><th>Opened / resolved / reopened / persisting</th><th>Comparison</th><th></th></tr></thead><tbody>'+[...runs].reverse().map(r=>'<tr><td>'+date(r.generatedAt)+'</td><td>'+esc(r.scanType)+'</td><td>'+r.pageCount+'</td><td>'+r.criticalCount+' / '+r.warningCount+' / '+r.infoCount+'</td><td>'+delta(r,'OPENED')+' / '+delta(r,'RESOLVED')+' / '+delta(r,'REOPENED')+' / '+delta(r,'PERSISTING')+'</td><td><span class="badge '+(r.comparisonStatus==='COMPARABLE'?'':'warn')+'">'+esc(r.comparisonStatus)+'</span><br><small>'+esc(r.comparisonNotes??'Same comparison-affecting configuration')+'</small></td><td><a class="button" style="display:inline-block;margin-right:7px;text-decoration:none" href="/history/runs/'+encodeURIComponent(r.id)+'">Open full report</a><button class="danger" data-delete="'+r.id+'">Delete</button></td></tr>').join('')+'</tbody></table></section>';document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>confirmRun(b.dataset.delete))}
function show(title,html,action){$('#modal-title').textContent=title;$('#modal-body').innerHTML=html;$('#confirm-action').onclick=action;modal.showModal()}function confirmRun(id){show('Delete this run permanently?','<p>This removes its database records and report folder. It cannot be undone.</p><label class="field">Type <b>DELETE '+esc(id)+'</b><input id="confirmation"></label>',async()=>{const r=await fetch('/api/v1/trends/runs/'+id,{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({confirmation:$('#confirmation').value})});const d=await r.json();if(!r.ok)return alert(d.error);modal.close();loadTrend(trend.id)})}
$('#choose-domain').onclick=()=>{$('#domain-search').value='';renderDomainPicker();$('#domain-picker').showModal()};$('#domain-search').oninput=renderDomainPicker;$('#delete-domain').onclick=()=>{if(!trend)return;show('Delete all domain history permanently?','<p>This removes every retained run and every corresponding report folder for <b>'+esc(trend.normalizedDomain)+'</b>.</p><label class="field">Type <b>DELETE '+esc(trend.normalizedDomain)+'</b><input id="confirmation"></label>',async()=>{const r=await fetch('/api/v1/trends/domains/'+trend.id,{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({confirmation:$('#confirmation').value})});const d=await r.json();if(!r.ok)return alert(d.error);location.reload()})};$('#merge').onclick=()=>{if(!trend||domains.length<2)return alert('At least two domain histories are required.');mergeSourceId=null;$('#confirm-merge').disabled=true;$('#merge-search').value='';$('#merge-reason').value='';$('#merge-help').innerHTML='Choose one source history to merge into <b>'+esc(trend.normalizedDomain)+'</b>. The source aliases and retained runs will move to this destination.';renderMergePicker();$('#merge-picker').showModal()};$('#merge-search').oninput=renderMergePicker;$('#confirm-merge').onclick=async()=>{if(!mergeSourceId)return;const reason=$('#merge-reason').value.trim();if(!reason)return alert('Enter a reason for this domain merge.');const source=domains.find(d=>d.id===mergeSourceId);if(!confirm('Merge all retained history for '+source.displayDomain+' into '+trend.normalizedDomain+'?'))return;const r=await fetch('/api/v1/trends/domains/'+trend.id+'/merge',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({sourceDomainId:mergeSourceId,reason})}),d=await r.json();if(!r.ok)return alert(d.error);location.reload()};loadDomains();
</script><script>
const competitorButton=document.createElement('button');competitorButton.id='competitors';competitorButton.textContent='Competitors';const intelligenceLinks=document.createElement('span');intelligenceLinks.innerHTML=' · <a href="/competitive">Competitive intelligence</a> · <a href="/ai-intelligence">AIO/AEO/GEO intelligence</a>';document.querySelector('header a').after(intelligenceLinks);
document.head.insertAdjacentHTML('beforeend','<style>.card{overflow:visible}.card .metric-source{cursor:pointer}.metric-source:after{content:attr(data-tooltip);position:absolute;z-index:100;right:-6px;bottom:calc(100% + 10px);width:max-content;max-width:min(300px,80vw);padding:9px 11px;border-radius:8px;background:#102d3d;color:white;font:600 12px/1.4 Inter,system-ui,sans-serif;letter-spacing:0;text-align:left;text-transform:none;white-space:normal;box-shadow:0 8px 24px #102d3d40;opacity:0;visibility:hidden;transform:translateY(4px);transition:opacity .15s,transform .15s,visibility .15s;pointer-events:none}.metric-source:hover:after,.metric-source:focus:after,.metric-source:focus-visible:after{opacity:1;visibility:visible;transform:translateY(0)}.card:has(.metric-source:hover),.card:has(.metric-source:focus){z-index:5}</style>');
document.head.insertAdjacentHTML('beforeend','<style>.metric-source:after{content:none!important}.scope-tooltip-portal{position:fixed;z-index:2000;display:none;max-width:min(320px,calc(100vw - 16px));padding:9px 11px;border-radius:8px;background:#102d3d;color:white;font:600 12px/1.4 Inter,system-ui,sans-serif;box-shadow:0 8px 24px #102d3d40;pointer-events:none}</style>');const scopeTooltip=document.createElement('div');scopeTooltip.className='scope-tooltip-portal';scopeTooltip.setAttribute('role','tooltip');document.body.append(scopeTooltip);const showScopeTooltip=target=>{scopeTooltip.textContent=target.dataset.tooltip||target.getAttribute('aria-label');scopeTooltip.style.display='block';const anchor=target.getBoundingClientRect(),box=scopeTooltip.getBoundingClientRect(),left=Math.max(8,Math.min(innerWidth-box.width-8,anchor.left+anchor.width/2-box.width/2)),above=anchor.top-box.height-8;scopeTooltip.style.left=left+'px';scopeTooltip.style.top=(above>=8?above:Math.min(innerHeight-box.height-8,anchor.bottom+8))+'px'};const tooltipSelector='.metric-source,.connectivity-item';document.addEventListener('mouseover',event=>{const target=event.target.closest?.(tooltipSelector);if(target)showScopeTooltip(target)});document.addEventListener('mouseout',event=>{if(event.target.closest?.(tooltipSelector))scopeTooltip.style.display='none'});document.addEventListener('focusin',event=>{const target=event.target.closest?.(tooltipSelector);if(target)showScopeTooltip(target)});document.addEventListener('focusout',event=>{if(event.target.closest?.(tooltipSelector))scopeTooltip.style.display='none'});addEventListener('resize',()=>scopeTooltip.style.display='none');addEventListener('scroll',()=>scopeTooltip.style.display='none',true);
const competitorDialog=document.createElement('dialog');competitorDialog.className='wide';competitorDialog.innerHTML='<div class="picker-head"><div><h2>Competitor domains</h2><div class="lead">Configure domains to crawl and compare. Crawl findings are directly observed; traffic, sessions, and users are not public first-party data and require an optional licensed estimate provider.</div></div><button class="icon-close" aria-label="Close competitor manager">×</button></div><form id="competitor-form" style="display:grid;grid-template-columns:1fr auto;gap:8px;margin:18px 0"><input class="search" style="margin:0" id="competitor-domain" required placeholder="competitor.example" aria-label="Competitor domain"><button>Add competitor</button></form><div id="competitor-results"></div>';document.body.append(competitorDialog);competitorDialog.querySelector('.icon-close').onclick=()=>competitorDialog.close();
async function loadCompetitors(){if(!trend)return;const response=await fetch('/api/v1/trends/domains/'+trend.id+'/competitors'),items=await response.json();if(!response.ok){document.querySelector('#competitor-results').innerHTML='<div class="empty">'+esc(items.error)+'</div>';return}document.querySelector('#competitor-results').innerHTML=items.length?'<table><thead><tr><th>Competitor</th><th>Audit status</th><th>Evidence</th><th></th></tr></thead><tbody>'+items.map(item=>'<tr><td><b>'+esc(item.normalizedDomain)+'</b></td><td>'+(item.auditedDomain?item.auditedDomain._count.runs+' retained audit'+(item.auditedDomain._count.runs===1?'':'s'):'Not audited yet')+'</td><td>Configured relationship; traffic estimates not enabled</td><td><a class="button" href="/?url='+encodeURIComponent('https://'+item.normalizedDomain)+'&settingsFrom='+encodeURIComponent(trend.id)+'">Audit with source settings</a> <button class="danger" data-remove-competitor="'+item.id+'">Remove</button></td></tr>').join('')+'</tbody></table>':'<div class="empty">No competitor domains configured.</div>';document.querySelectorAll('[data-remove-competitor]').forEach(button=>button.onclick=async()=>{await fetch('/api/v1/trends/domains/'+trend.id+'/competitors/'+button.dataset.removeCompetitor,{method:'DELETE'});loadCompetitors()})}
competitorButton.onclick=()=>{loadCompetitors();competitorDialog.showModal()};document.querySelector('#competitor-form').onsubmit=async event=>{event.preventDefault();const domain=document.querySelector('#competitor-domain').value.trim(),response=await fetch('/api/v1/trends/domains/'+trend.id+'/competitors',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({domain})}),data=await response.json();if(!response.ok)return alert(data.error);document.querySelector('#competitor-domain').value='';loadCompetitors()};
</script></body></html>`;
